services:
  postgres:
    image: postgres:16
    container_name: wad_postgres
    environment:
      POSTGRES_DB: wad
      POSTGRES_USER: wad
      POSTGRES_PASSWORD: wad
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7
    container_name: wad_redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

volumes:
  postgres_data:
  redis_data:
